Script to perform Newton's method.
When executed from the command line, it takes as arguments the function, the initial guess and the number of repetitions.

$ python newtonMethod.py function guess repetitions

Required packages: sympy, pytest, pep8
